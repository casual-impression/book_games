#include <iostream>
#include <string>
#include <random>
#include <ctime>
#include <algorithm>

int main() {
    // Set the locale to support Russian characters
    std::locale::global(std::locale(""));

    
    return 0;
}